## Summary

- 

## Test plan

- [ ] `mage83 module:enable Null_Blueprint && mage83 setup:upgrade` (or `php bin/magento` on the target host)
- [ ] Admin grid + form save
- [ ] Widget on a CMS page (campaign mode and custom mode)
- [ ] Storefront `/campaigns/` and `/campaigns/index/view/id/{id}`
- [ ] `dev/qa/vendor/bin/phpunit --testsuite unit`
